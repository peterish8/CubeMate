interface:
  display_name: "CubeMate"
  short_description: "Repo-specific patterns and workflows for CubeMate"
  default_prompt: "Use the CubeMate repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true